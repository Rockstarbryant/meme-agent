# Make sure you are back to origin/main
git reset --soft origin/main

# Stage the cleaned file + your updated .gitignore
git add backend/runner.env.example .gitignore

# Commit
git commit -m "fix: remove real OpenRouter key from runner.env.example and update .gitignore"

# Force-push
git push --force-with-lease